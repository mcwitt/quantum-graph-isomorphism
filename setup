#! /bin/bash

echo -n 'N=? '; read N
D=$((2**N))

build=build-n$N
mkdir -p $build

for f in *".in"
do
    sed "\
        s:@build@:$build:;\
        s:@N@:$N:;\
        s:@D@:$D:;\
        " < "$f" > "$(basename "${f%.in}")"
done

cp global.h $build
ln -s $(pwd)/graphs $build/graphs
